<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppThemeSecond" parent="@style/Theme.AppCompat.Light">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textCursorDrawable">@color/white</item>
        <item name="android:colorEdgeEffect">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>


    <style name="popup_window_animation">
        <item name="android:windowEnterAnimation">@anim/onshow_animation</item>
        <item name="android:windowExitAnimation">@anim/ondismiss_animation</item>
    </style>

    <style name="ScrollBarRecyclerView" parent="android:Widget">
        <item name="android:scrollbars">vertical</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.AppCompat.Small">
        <item name="android:textSize">25dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="MyHintText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/viewfinder_laser</item>
    </style>

    <style name="MyStyle" parent="TextAppearance.AppCompat.Small">
        <item name="gave_text">Test</item>
    </style>

    <style name="SwitchCompatStyle" >
        <item name="thumbTint">@color/white</item>
    </style>

</resources>
